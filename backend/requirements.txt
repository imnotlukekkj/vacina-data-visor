fastapi>=0.95.0
uvicorn[standard]>=0.22.0

# Optional (used only if you later connect to Supabase or use env files)
python-dotenv>=1.0.0
# asyncpg is optional and may fail to build on some Windows/Python versions.
# If you need Postgres support, install it in a compatible environment (e.g. Python 3.11)
# asyncpg>=0.27.0
httpx>=0.24.0
psycopg2-binary>=2.9.6